version: "3.9"
services:
  pg:
    image: pgvector/pgvector:pg16
    container_name: cloudai-pg
    environment:
      POSTGRES_PASSWORD: postgres
      POSTGRES_USER: postgres
      POSTGRES_DB: cloudai
    ports: ["5432:5432"]
    volumes:
      - ./pgdata:/var/lib/postgresql/data
